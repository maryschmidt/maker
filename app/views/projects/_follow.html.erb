<%= form_for(current_user.relationships.build(followed_id: @project.user.id)) do |f| %>
  <%= f.hidden_field :followed_id %>
  <%= @project.user.email %><br />
  <%= f.submit "Follow", class: "button-small" %>
<% end %>
